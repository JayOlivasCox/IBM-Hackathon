Steps:


Create virtual env:

    python -m venv venv
    source venv/bin/activate

Download libraries for call:

    pip install anthropic
    pip install python-dotenv

Don't forget the .gitignore files

Name the anthropic api key: "ANTHROPIC_API_KEY" <--- mandatory